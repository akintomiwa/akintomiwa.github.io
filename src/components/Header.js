import React from "react";

function Header(props) {
        console.log(props)
    // return <h1>Hello {props.name}, Target colour: {props.color}</h1>;
    return (
        <h1>CousinAkin.com</h1>
        // <h3>by {props.name}</h3>

    )
}

export default Header;